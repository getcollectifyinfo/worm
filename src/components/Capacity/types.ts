export interface CapacitySettings {
  planeSpeed: number;
  scrollSpeed: number;
  spawnRate: number;
  taskChangeSpeed: number;
  gameDuration: number;
  gapWidth: number;
}

export interface CapacityStats {
  hits: number;
  targets: number;
  fails: number;
}
